0.30 2026-05-22
    - hardened the managed Telegram reply worker so verbose-progress reporter
      failures and progress-callback failures no longer abort the active
      Codex reply mid-operation
    - added per-session audit support through
      `dashboard telegram-codex.start --audit`, writing
      `~/.telegram-codex/<session-id>/audit.enabled` and `audit.jsonl`
    - upgraded the real `codex exec resume` path to capture stderr tail,
      exit status, and streamed progress events so empty mid-run failures can
      be diagnosed instead of collapsing into a generic reply error

0.29 2026-05-21
    - replaced the placeholder Telegram progress heartbeat with real
      step-by-step verbose streaming from `codex exec resume --json`
    - streamed agent messages and command execution events into one preserved
      Telegram trace while keeping `typing...` active until final delivery

0.28 2026-05-21
    - attached downloaded Telegram photos and image documents to resumed
      Codex replies as real `codex exec resume -i` image inputs instead of
      only describing them by local path in the prompt
    - narrowed the managed media contract so audio, voice, video, PDF, and
      other non-image files stay local-path inputs for tool-based inspection
      instead of being described as direct binary model attachments

0.27 2026-05-21
    - stopped `dashboard telegram-codex.start` from treating ambient
      workspace `OLLAMA_MODEL` as an automatic provider switch, so managed
      startup now keeps the real Codex launch path unless Telegram-owned
      Ollama routing is explicitly requested
    - changed the explicit Ollama branch to inject Codex `--profile
      ollama-launch -m ...` arguments directly into the real Codex exec path
      instead of recursively calling `ollama launch codex` through the
      wrapped `codex` command again
    - prevented repeated `resume <session>` prefixes when the incoming Codex
      argv already carries a real resume target
    - made collector startup side effects idempotent inside an already
      running managed Codex process tree through a start-reentry guard
    - made `telegram-codex.start` heal stale wrong-session
      `telegram-codex-*` collector entries that still point at the current
      workspace

0.26 2026-05-21
    - changed `dashboard telegram-codex.start --version` to proxy the real
      underlying Codex CLI version output instead of the skill version so DD
      launcher validation accepts the wrapped command
    - kept the `--version` CLI path side-effect free so repeated DD probes
      still do not create or restart collectors

0.25 2026-05-21
    - changed the top-level `dashboard telegram-codex.start --version` CLI
      output from JSON to a raw semver line so DD launcher checks can parse it
      correctly
    - kept the start-version path side-effect free so version probes still do
      not create or restart collectors

0.24 2026-05-21
    - made `dashboard telegram-codex.start --version` a side-effect-free
      version query so DD launcher probes no longer create or restart managed
      Telegram collectors
    - changed the real Codex and Ollama launch path to `exec` instead of
      `system(...)` so successful managed startup no longer leaves an extra
      resident `cli/start` wrapper process waiting in the background

0.23 2026-05-21
    - removed managed Telegram reply prefaces by telling resumed Codex
      sessions to answer directly without greetings, acknowledgements, or
      status boilerplate unless the user explicitly asked for them
    - made managed Telegram task requests require real in-session work before
      replying, and retried once with a stricter prompt when Codex returned a
      promise-only placeholder such as `will be done`
    - added fallback recovery of the real Codex target session from
      `~/.developer-dashboard/config/codex.json` when the per-session
      `codex.session` file is missing
    - added managed Telegram in-progress status streaming for longer task
      requests while the resumed Codex session is still working

0.22 2026-05-21
    - kept the managed collector-owned Telegram `typing...` indicator alive
      through the full Codex reply lifecycle, including the final outbound
      Telegram send, instead of stopping as soon as reply text generation
      finished
    - added Docker-verified TDD coverage proving the typing guard now stays
      active until the final `sendMessage` attempt completes and still cleans
      up correctly when reply generation or final delivery fails

0.21 2026-05-21
    - fixed Telegram Bot API `getFile` requests to send query-string
      parameters instead of mistaken HTTP headers so inbound photo, PDF,
      audio, and other file downloads now work in both the public
      `dashboard telegram-codex.download` command and the managed
      collector-owned media reply path
    - added Docker-verified TDD coverage proving `telegram_get` keeps
      parameters in the URL query instead of mis-sending them as request
      headers

0.20 2026-05-20
    - made the managed collector-owned `check-message` worker download inbound
      photo, document, audio, video, and voice files into the session runtime
      before asking Codex to reply
    - exposed the downloaded `*_local_path` values in the Codex reply prompt
      and explicitly told Codex those files are already available locally for
      inspection
    - added outbound managed attachment directives so Codex replies can send
      photos, audio, and documents back to Telegram instead of only plain text
    - added the public `dashboard telegram-codex.send-audio` command and
      Docker-verified coverage for the expanded media workflow

0.19 2026-05-20
    - made the collector-owned managed Telegram reply path send
      `sendChatAction` with `typing` before Codex generates the final reply
    - kept typing-status failures non-fatal so Telegram reply generation and
      delivery still continue
    - added Docker-verified regressions for typing-action ordering and
      non-blocking typing-action failures

0.18 2026-05-20
    - made the collector-owned `dashboard telegram-codex.check-message
      <session-id>` worker persist any recovered inbox-ledger next offset
      back to `listener.offset` before polling so stale on-disk offset state
      cannot keep misleading restart diagnostics or replay investigations
    - added Docker-verified regressions proving offset recovery rewrites
      `listener.offset` both when the file is missing and when the stored
      offset is older than the inbox-ledger truth

0.17 2026-05-20
    - made the collector-owned `dashboard telegram-codex.check-message
      <session-id>` worker bind its explicit session suffix back into the
      runtime env so replies and state stay under the intended
      `~/.telegram-codex/<session-id>/` tree
    - made collector-owned polling auto-switch into Codex-session reply mode
      when `codex.session` exists for that session, instead of requiring a
      separate listener-mode env override
    - made the spawned `codex exec resume` reply subprocess use
      `--dangerously-bypass-approvals-and-sandbox` so the automated Telegram
      reply handoff does not hang in the default Linux sandbox path
    - added Docker-verified regressions for legacy plural collector command
      rewrite, persisted-session collector replies, stale pid cleanup, and
      real `kill 0` process detection fallback

0.16 2026-05-20
    - changed the managed collector command to the session-suffixed form
      `dashboard telegram-codex.check-message <session-id>`
    - added a same-session pid guard so a second
      `telegram-codex.check-message <session-id>` process exits without
      starting duplicate polling work
    - tightened collector healing so same-name collectors with the wrong
      `cwd` are rewritten to the current workspace during
      `dashboard telegram-codex.start`

0.15 2026-05-20
    - moved the always-on Telegram polling path under a DD collector owned by
      `dashboard telegram-codex.start` instead of keeping the standalone
      resident listener as the primary runtime model
    - made `telegram-codex.start` heal `~/.developer-dashboard/config/config.json`
      so exactly one `telegram-codex-<session-id>` collector exists per
      workspace session, with duplicate collector entries removed automatically
    - added `dashboard telegram-codex.check-messages` and `./cli/check-messages`
      as the collector-owned long-running polling loop, while persisting the
      active Codex reply target in `~/.telegram-codex/<session-id>/codex.session`

0.14 2026-05-20
    - made managed listener startup `exec` the real `cli/listen` process so
      one session keeps one actual listener pid
    - added session-aware running-listener detection so managed startup can
      reuse an existing listener instead of spawning a duplicate for the same
      session
    - blocked duplicate Telegram replies by skipping any update whose
      `update_id` is already present in the session inbox ledger

0.13 2026-05-20
    - made managed startup exec the skill-owned `cli/listen` directly instead
      of launching the listener through a nested `dashboard telegram-codex.listen`
      process chain
    - fixed the managed listener pid tracking so `listener.pid` now follows the
      real resident listener process instead of an intermediate startup process
    - added Docker-verified TDD coverage for the direct skill-owned listener
      launch path

0.12 2026-05-20
    - fixed `telegram-codex.listen 0 ...` so `0` once again means
      "stay running forever" instead of exiting after the first poll cycle
    - kept managed `telegram-codex.start` aligned with that run-forever
      listener behavior because it launches `telegram-codex.listen 0 30`
    - added Docker-verified TDD coverage for the zero-cycle sentinel path

0.11 2026-05-20
    - routed managed `telegram-codex.start` listener replies through the
      active Codex session by resuming that session and asking Codex to
      generate the Telegram reply text
    - removed the managed startup placeholder/acknowledgement as the actual
      two-way Telegram reply path
    - kept direct `telegram-codex.listen` available for passive capture or
      explicit static acknowledgements when requested

0.10 2026-05-20
    - made `dashboard telegram-codex.start` launch the listener in a stable
      two-way mode with a concise one-line Telegram acknowledgement for new
      inbound messages instead of the old passive startup behavior
    - kept direct `telegram-codex.listen` passive by default unless an
      explicit reply text is passed
    - made the listener survive transient `getUpdates` transport failures and
      clamp stale stored offsets up to the newer inbox-ledger offset so
      duplicated older Telegram updates are not replayed

0.09 2026-05-20
    - removed the listener's default placeholder auto-reply so
      `telegram-codex.listen` now captures inbound Telegram updates by
      default instead of sending `queued for Codex` bot messages
    - kept explicit listener acknowledgements available when a reply text
      is passed on the command line
    - updated the docs and Docker-verified TDD coverage to reflect the new
      passive-by-default listener behavior

0.08 2026-05-20
    - blocked duplicate Telegram reply spam by skipping any returned update
      whose `update_id` is older than the next stored session offset
    - added inbox-ledger offset recovery so a missing `listener.offset` file
      does not force the listener to replay stale Telegram backlog
    - added Docker-verified TDD coverage for stale-update skipping and
      inbox-ledger offset recovery in the always-on listener path

0.07 2026-05-20
    - finished the skill-owned `telegram-codex.start` launcher slice with
      Docker-verified coverage closure for the preserved saved-session and
      listener startup paths
    - fixed the listener anti-spam failure mode so a failed Telegram
      acknowledgement still advances the offset instead of replaying the
      same inbound message on every restart
    - kept the generated MCP bridge Python source as a standalone
      `scripts/telegram_mcp.py` asset instead of embedding it inside Perl
    - documented the covered-gate handling for the intentional child stdio
      redirection statements used only inside the background listener fork

0.06 2026-05-20
    - moved the real Telegram-aware Codex startup flow into
      `dashboard telegram-codex.start` and `./cli/start`
    - reduced `~/.developer-dashboard/cli/codex` and the user-PATH `codex`
      helper to thin handoff launchers into the skill-owned start command
    - preserved the original saved-session resume mapping from
      `TICKET_REF` and `~/.developer-dashboard/config/codex.json` inside
      the skill-owned start logic
    - added session-scoped listener pid/log paths to the runtime layout
      and updated the Docker-verified regressions and docs for the merged
      launcher ownership model

0.05 2026-05-20
    - fixed Codex auto-start integration so `dashboard skills install
      telegram-codex` provisions a managed `codex` wrapper in the user PATH
      instead of assuming `~/.developer-dashboard/cli/codex` is the active
      launcher
    - made managed `codex` auto-start prime the Telegram offset to the latest
      pending update on first start so old backlog messages do not get
      auto-replied and spam the chat
    - added a skill `Makefile` install hook so normal skill installation is
      enough to set up Telegram listener auto-start for later `codex` launches
    - updated the docs and Docker-verified regressions to prove the managed
      `codex` command path, session pid/log files, no-backlog auto-start
      behavior, and auto-setup behavior

0.04 2026-05-20
    - fixed the install docs to use `dashboard skills install telegram-codex`
      instead of a checkout path
    - made `dashboard telegram-codex.install` also write a `codex` launcher
      wrapper under `~/.developer-dashboard/cli/` that starts the Telegram
      listener automatically for the active Codex session before execing the
      real Codex binary
    - added Docker-verified regressions for the generated Codex wrapper,
      including session pid/log paths and real-binary handoff

0.03 2026-05-20
    - partitioned listener runtime state by Codex session id under
      `~/.telegram-codex/<session-id>/` so each Codex session keeps its own
      offset file and inbox ledger
    - added session-id precedence for `TELEGRAM_CODEX_SESSION_ID` over
      `CODEX_SESSION_ID`, with a safe `default` fallback when no session id
      is available
    - updated the listener docs, agent handoff guide, and Docker-verified
      regressions to prove session-specific state paths and listener output

0.02 2026-05-20
    - added `dashboard telegram-codex.listen` and `./cli/listen` for a
      long-poll always-on listener with persistent update offset state and
      local inbox ledger capture
    - made `TELEGRAM_BOT_TOKEN` discovery search the active project `.env`,
      parent/root `.env`, skill `.env`, and process environment so
      `dashboard telegram-codex...` can pick it up without an explicit export
    - documented the continuous listener workflow and bounded listener test
      mode, and added Docker-verified regression coverage for listener state,
      inbox logging, and executable wrapper behavior

0.01 2026-05-20
    - added the first packaged `telegram-codex` skill with install, get-me,
      updates, download, reply, send-photo, send-document, and `/start`
      auto-reply commands for a local Codex Telegram MCP bridge
    - added a generated stdio MCP plugin scaffold for Codex local plugin
      roots and local marketplace registration
    - added Docker-verified tests and 100% coverage proof for scaffold,
      Telegram API request shaping, CLI behavior, and release metadata
    - fixed the shipped CLI wrapper modes so the skill checkout entrypoints
      are executable and covered by regression tests
    - proved the live bot flow with install, get-me, updates, `/start`
      auto-reply, and direct reply evidence
