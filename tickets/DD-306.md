# DD-306

## Summary

Replace transcript-only Telegram/TUI sharing with a real live shared session path so Telegram-originated turns and TUI-originated turns stay in sync while they are happening, not only after persisted transcript reuse.

## Method

TDD

## Acceptance Criteria

- when the mapped Codex session is already open in a tmux-backed TUI, Telegram-managed inbound messages are injected into that same live session instead of only spawning detached `codex exec resume` work beside it
- an already-open Codex TUI attached to that same mapped session shows the Telegram-originated turn and its live progress in-session
- Telegram replies continue to receive live progress and final delivery from that same shared live session
- TUI-originated work is mirrored back to the paired Telegram chat from the same transcript stream
- when no live tmux-backed TUI session is available, the runtime falls back cleanly to the existing detached `codex exec resume` plus persisted-transcript path
- docs explain the live-sync runtime model and any required startup contract precisely
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
