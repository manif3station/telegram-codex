# DD-278

## Summary

Remove the listener's default placeholder Telegram auto-reply so passive inbox capture is the default behavior.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.listen` captures inbound Telegram updates by default without sending a placeholder auto-reply
- the listener still supports explicit acknowledgement replies when a reply text is passed on the command line
- `dashboard telegram-codex.auto-reply-start` remains available for explicit `/start` replies
- docs explain that automatic substantive Telegram replies are not generated by the listener itself
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
