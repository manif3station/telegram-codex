# DD-297

## Summary

Replace the placeholder Telegram progress message with real step-by-step Codex verbose event streaming from `codex exec resume --json`.

## Method

TDD

## Acceptance Criteria

- managed `dashboard telegram-codex.check-message <session-id>` streams actual `codex exec resume --json` progress events into Telegram instead of the placeholder `Codex is still working on your request...`
- streamed progress includes real agent-message and command-execution steps from the resumed Codex session
- the streamed progress message is updated in place and preserved as a useful step trace instead of being deleted as a generic heartbeat
- typing status still remains active until the final outbound Telegram reply send attempt completes
- docs explain the verbose step-streaming behavior and how it differs from the old placeholder progress message
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
