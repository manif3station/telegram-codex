# DD-300

## Summary

Restore live Telegram verbose progress for broader task-style requests and emit an immediate kickoff line before delayed Codex JSON events arrive.

## Method

TDD

## Acceptance Criteria

- task-style requests such as `Run all the tests and check if any test not good enough` open the managed Telegram verbose stream instead of skipping straight to the final reply
- managed Telegram verbose mode emits an immediate kickoff line before richer `codex exec resume --json` events arrive
- minimal event streams that include `thread.started` still surface a useful Telegram progress line
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
