# DD-286

## Summary

Add a Telegram typing indicator to the managed collector-owned reply path so users can see that Codex is actively processing an inbound Telegram message before the final reply is sent.

## Method

TDD

## Acceptance Criteria

- the collector-owned `dashboard telegram-codex.check-message <session-id>` worker sends Telegram `sendChatAction` with `typing` before it asks Codex to generate a reply for a text or caption-bearing inbound message
- the worker does not send duplicate or pointless typing actions for updates that will not produce a reply
- typing-status failures do not block the real reply path
- docs explain that managed Telegram replies now surface a Telegram typing indicator while Codex is processing
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
