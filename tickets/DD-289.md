# DD-289

## Summary

Keep the managed collector-owned Telegram `typing...` indicator active until the final outbound Telegram reply send attempt finishes, instead of stopping as soon as Codex finishes generating reply text.

## Method

TDD

## Acceptance Criteria

- the collector-owned `dashboard telegram-codex.check-message <session-id>` worker keeps Telegram `sendChatAction` with `typing` active until the final outbound Telegram reply send attempt finishes, not only until reply text generation ends
- typing guard cleanup still runs when Codex reply generation fails or when the final Telegram send fails
- docs explain that the managed typing indicator now covers final delivery, not only reply generation
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
