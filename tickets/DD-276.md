# DD-276

## Summary

Move the real Telegram-aware Codex startup logic into `telegram-codex.start` / `cli/start`, make `~/.developer-dashboard/cli/codex` plus any PATH-level `codex` helper thin handoff launchers into that skill entrypoint, and close the listener replay-spam failure mode in the same governed slice.

## Method

TDD

## Acceptance Criteria

- `telegram-codex` exposes `dashboard telegram-codex.start` as the skill-owned start entrypoint
- `cli/start` owns the real session mapping and Telegram listener startup logic
- `telegram-codex.start` preserves the original `~/.developer-dashboard/cli/codex` saved-session resume behavior driven by `TICKET_REF` and `~/.developer-dashboard/config/codex.json`
- `~/.developer-dashboard/cli/codex` becomes a thin handoff into `dashboard telegram-codex.start`
- any PATH-level `codex` helper installed by the skill becomes a thin handoff into `~/.developer-dashboard/cli/codex`
- first auto-start with no stored offset still primes to the latest Telegram update and does not auto-reply to old backlog
- docs explain the merged launcher ownership clearly
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
