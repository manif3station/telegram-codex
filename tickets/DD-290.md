# DD-290

## Summary

Remove managed Telegram reply prefaces, force task-style Telegram requests to do real in-session work before replying, and recover the saved Codex target session from `codex.json` when `codex.session` is missing.

## Method

TDD

## Acceptance Criteria

- managed Telegram reply prompts tell Codex to answer directly without boilerplate prefaces or empty acknowledgements unless the user explicitly asked for them
- managed Telegram task requests tell Codex to do the actual work in-session before replying and explicitly block promise-only replies such as `will be done`
- if the managed reply path receives a promise-only placeholder for a task request, it retries once with a stricter completion prompt instead of sending that placeholder to Telegram
- when `~/.telegram-codex/<session-id>/codex.session` is missing, the managed reply path can recover the target Codex session from `~/.developer-dashboard/config/codex.json` using the same saved-session mapping logic as `telegram-codex.start`
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
