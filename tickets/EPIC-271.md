# EPIC-271

## Summary

Add a governed `telegram-codex` skill that bridges Telegram Bot API into a local Codex plugin and stdio MCP server, then extend it with an always-on listener path, per-Codex-session runtime state, and a managed `codex` launcher path that starts the session listener automatically while preserving the original saved-session resume flow.
