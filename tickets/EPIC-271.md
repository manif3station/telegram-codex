# EPIC-271

## Summary

Add a governed `telegram-codex` skill that bridges Telegram Bot API into a local Codex plugin and stdio MCP server, then extend it with DD-collector-owned always-on polling, per-session runtime state, pairing security so only a locally paired Telegram chat can drive a session, a managed `codex` launcher path that starts the Telegram collector automatically while preserving the original saved-session resume flow, replay guards that stop stale Telegram backlog from being appended or replied again, managed Telegram typing indicators that stay active through final reply delivery, always-on verbose step streaming from `codex exec resume --json` with immediate kickoff lines before delayed Codex events arrive, managed inbound/outbound media handling through downloaded local files plus attachment reply directives, real image-input handoff for downloaded Telegram photos and image documents through `codex exec resume -i`, direct no-preface task replies that do the actual in-session work before responding, fallback reply-session recovery from `codex.json` when `codex.session` is missing, reuse of the persisted Codex session transcript as shared reply context plus readable journaling of Telegram turns back into that transcript, a verified `getFile` query-string path so real inbound Telegram media downloads work in live collector flows, a probe-safe `telegram-codex.start --version` plus direct `exec` handoff so DD discovery does not churn collector side effects, leave extra wrapper processes behind, fail DD launcher version parsing, or report the wrapped skill version instead of the real underlying Codex CLI version, startup recursion guards that keep ambient workspace provider env plus nested wrapped Codex calls from re-entering `telegram-codex.start` and repeatedly restarting collectors, an audited managed reply path that records progress-stream failures plus real `codex exec resume` exit context instead of silently cutting off mid-operation, managed startup recycling that replaces stale already-running per-session workers so new audit and progress behavior takes effect immediately, and a tmux-backed live shared session model so Telegram and the active Codex TUI can stay synchronized during the same turn instead of only after transcript persistence.
