# DD-288

## Summary

Fix Telegram Bot API `getFile` requests so both direct downloads and managed collector-owned media replies can fetch real inbound Telegram files without `file_id not specified` failures.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.download <FILE_ID>` sends `getFile` parameters in the URL query string instead of mistaken HTTP headers
- the managed collector-owned `dashboard telegram-codex.check-message <session-id>` worker can download real inbound supported Telegram media before asking Codex to reply
- inbound photo, PDF, audio, voice, video, and other file downloads no longer fail with `Bad Request: file_id not specified`
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
