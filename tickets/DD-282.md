# DD-282

## Summary

Launch the managed Telegram listener directly from the skill-owned `cli/listen` entrypoint so `listener.pid` tracks the real resident listener process instead of an intermediate startup wrapper.

## Method

TDD

## Acceptance Criteria

- managed `dashboard telegram-codex.start` launches the skill-owned `cli/listen` command directly
- `listener.pid` tracks the real resident listener process instead of an intermediate startup process
- docs explain the direct managed listener launch path clearly
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
