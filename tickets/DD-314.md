# DD-314

## Summary

Restore real two-way live sync between Telegram and the active Codex TUI by fixing the tmux submit keystroke used for Telegram-injected turns and by making outbound TUI transcript mirroring run even when Telegram polling is idle or transiently failing.

## Method

TDD

## Acceptance Criteria

- inbound Telegram turns injected into a live tmux-backed Codex TUI session are actually submitted instead of staying stranded in the TUI prompt
- the collector-owned live-pane path no longer falls back with `Live Codex pane never recorded the injected Telegram turn` for ordinary text prompts
- TUI-originated turns are mirrored back to the paired Telegram chat even when `getUpdates` is idle or hits a transient transport failure
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
