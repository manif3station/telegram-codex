# DD-275

## Summary

Prevent spam on listener auto-start by priming the Telegram offset to the latest update when no stored offset exists, so old backlog messages do not get auto-replied.

## Method

TDD

## Acceptance Criteria

- first auto-start with no stored offset primes the listener to the latest Telegram update instead of replying to old backlog messages
- later listener cycles still reply normally to new inbound messages
- manual listener runs without the auto-start priming flag keep the existing backlog-processing behavior
- docs explain the no-backlog-reply rule for managed `codex` auto-start
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
