# DD-308

## Summary

Keep unpaired Telegram trigger messages out of the live Codex TUI and shared Codex transcript. The first unpaired inbound message should only generate the local pair command and must not pollute the active Codex session.

## Method

TDD

## Acceptance Criteria

- the first unpaired Telegram trigger message still gets exactly one local `d2 telegram-codex.pair <hexcode>` reply
- unpaired trigger messages do not resume Codex
- unpaired trigger messages do not inject into a live tmux-backed Codex TUI pane
- unpaired trigger messages do not append into the shared Codex session transcript
- paired chats still resume normal Codex/TUI behavior after local pairing
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
