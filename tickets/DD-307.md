# DD-307

## Summary

Stop the live Telegram/TUI sync path from stalling when `telegram-codex` picks a stale `codex resume <session-id>` process or when the live pane never records the injected Telegram turn.

## Method

TDD

## Acceptance Criteria

- live-pane discovery prefers the freshest tmux-backed `codex resume <session-id>` process instead of the first stale matching process
- if a live pane never records the injected Telegram turn, the worker fails fast instead of waiting the full live timeout
- when the live-pane path fails, `telegram-codex` falls back cleanly to detached `codex exec resume` and still returns a Telegram reply
- audit records the live-pane fallback reason clearly
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
