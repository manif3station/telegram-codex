# DD-313

## Summary

Prune stale duplicate `codex resume <session-id>` processes for collector-owned sessions so `telegram-codex` does not keep accumulating orphan live-session processes on the same tty and so live-pane discovery stays aligned with the freshest real session.

## Method

TDD

## Acceptance Criteria

- collector-owned `telegram-codex` sessions identify stale duplicate `codex resume <session-id>` processes that share the same tty as a fresher live session
- only clearly stale orphan duplicates are pruned, without killing the freshest live session process for that tty
- live-pane discovery continues to prefer the freshest valid session process after pruning
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
