# DD-312

## Summary

Harden Telegram slash command parsing so supported commands such as `/status` and `/help` are still handled directly when Telegram text includes surrounding whitespace or newline noise instead of being forwarded into Codex as ordinary prompt text.

## Method

TDD

## Acceptance Criteria

- paired Telegram slash commands such as `/status` and `/help` are still detected when the Telegram text has leading or trailing whitespace or newline noise
- normalized slash commands continue to bypass the managed Codex prompt path and are handled directly by `telegram-codex`
- ordinary non-slash Telegram text still does not get misclassified as a slash command
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
