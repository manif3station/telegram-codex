# DD-295

## Summary

Stop `telegram-codex.start` from recursively re-entering and restarting collectors when ambient workspace provider env or nested wrapped Codex calls are present.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start` ignores ambient workspace `OLLAMA_MODEL` so Telegram-managed startup does not silently switch provider or recurse through `ollama launch codex`
- explicit Telegram-owned Ollama startup still works through a dedicated skill env contract without re-entering the managed `codex` wrapper
- saved-session resume mapping is not prepended a second time when incoming Codex argv already carries a real `resume <session>` target
- nested managed `codex` invocations in the same process tree do not keep re-running collector restart side effects
- stale same-workspace `telegram-codex-*` collector entries with the wrong session id are healed away during startup
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
