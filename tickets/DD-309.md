# DD-309

## Summary

Make `telegram-codex.start` launch the managed Codex process with `--dangerously-bypass-approvals-and-sandbox` so the Telegram-owned runtime and the direct managed start path use the same non-interactive execution contract.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start` prepends `--dangerously-bypass-approvals-and-sandbox` when it launches the real managed Codex process
- the managed launch path does not duplicate that flag when it is already present
- the explicit Telegram-owned Ollama launch profile path preserves the same managed no-approval flag on the final Codex argv
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
