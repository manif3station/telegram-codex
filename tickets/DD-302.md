# DD-302

## Summary

Add pairing security so only a locally paired Telegram chat can drive a managed `telegram-codex` session.

## Method

TDD

## Acceptance Criteria

- the first unpaired inbound Telegram message receives exactly one local pairing command reply in the form `d2 telegram-codex.pair <hexcode>`
- later unpaired messages are ignored until the local pair command runs
- `dashboard telegram-codex.pair <hexcode>` pairs the pending Telegram chat to the current workspace session
- after pairing, only that paired Telegram chat can drive the session and outsider chats are ignored
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
