# DD-280

## Summary

Route inbound Telegram messages through the active Codex session so Telegram replies are generated by Codex within the live session context instead of coming from a static placeholder acknowledgement.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start` no longer uses a static placeholder or acknowledgement as the real Telegram reply path
- managed listener mode resumes the active Codex session and asks Codex to generate the Telegram reply text within that session context
- the managed listener sends the Codex-generated reply back to Telegram for inbound text messages instead of a placeholder
- direct `dashboard telegram-codex.listen` remains available for passive capture or explicit static acknowledgement when requested
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
