# DD-283

## Summary

Enforce one live listener per session and skip Telegram updates already recorded in the session inbox ledger so duplicate listeners cannot slowly replay older messages.

## Method

TDD

## Acceptance Criteria

- managed startup reuses the existing listener for the same session instead of spawning a duplicate
- the listener pid file follows the real resident listener process for that session
- the listener skips any Telegram update whose `update_id` is already present in the session inbox ledger
- docs explain the single-listener and duplicate-update guard behavior clearly
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
