# DD-272

## Summary

Partition `telegram-codex` listener runtime state by Codex session id so each Codex session keeps its own Telegram offset and inbox history under `~/.telegram-codex/`.

## Method

TDD

## Acceptance Criteria

- listener runtime state under `~/.telegram-codex/` is partitioned by Codex session id
- each Codex session gets its own offset file and inbox history ledger
- `dashboard telegram-codex.listen` and `./cli/listen` report the session-specific runtime paths they used
- `.env` token discovery continues to work from root, project, or skill `.env`
- docs explain the per-session runtime layout and supported Telegram media flows clearly for reused Codex sessions
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
