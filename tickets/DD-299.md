# DD-299

## Summary

Recycle any already-running per-session `telegram-codex.check-message <session-id>` worker during managed startup so new audit and progress behavior actually takes effect instead of being masked by a stale long-lived worker.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start` recycles any already-running per-session `check-message <session-id>` worker before restarting the DD collector
- restarting with new behavior such as `--audit` or newer progress-stream logic replaces the stale long-lived worker instead of leaving it running for hours on old code
- docs explain that managed startup now refreshes the live per-session worker so new audit and progress behavior takes effect immediately
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
