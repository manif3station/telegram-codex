# DD-292

## Summary

Stop `telegram-codex.start` probe churn and extra wrapper processes by making `--version` a side-effect-free metadata query and replacing the real Codex or Ollama launch path with `exec`.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start --version` returns version metadata without creating or restarting collectors
- repeated DD command discovery probes against `cli/start --version` do not trigger managed startup side effects
- the real Codex or Ollama launch path replaces the wrapper process instead of leaving a long-lived extra `cli/start` parent waiting on `system(...)`
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
