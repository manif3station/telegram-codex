# DD-296

## Summary

Attach downloaded Telegram images to resumed Codex sessions as real image inputs instead of only passing local file paths in prompt text.

## Method

TDD

## Acceptance Criteria

- managed `dashboard telegram-codex.check-message <session-id>` passes downloaded Telegram photos into resumed Codex replies as real `codex exec resume -i <image>` inputs instead of only mentioning local file paths in prompt text
- downloaded Telegram image documents also flow into resumed Codex replies as real image inputs when their MIME type or filename proves they are images
- non-image binary media such as audio, voice, video, and PDF documents are no longer described in docs or prompt contracts as if the current skill can hand their bytes directly into the model
- the managed prompt and docs clearly distinguish real attached image inputs from downloaded local-path-only media
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
