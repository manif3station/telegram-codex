# DD-310

## Summary

Keep Telegram `typing...` active for TUI-originated turns that are mirrored back through the shared transcript stream, not only for Telegram-originated managed reply work.

## Method

TDD

## Acceptance Criteria

- when a paired Codex TUI turn is mirrored back to Telegram from the shared transcript stream, Telegram `typing...` stays active until the final outbound Telegram reply send completes
- the TUI-originated typing guard stays alive across multiple collector transcript polls until the final assistant turn arrives
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
