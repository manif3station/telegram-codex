# DD-301

## Summary

Make managed Telegram verbose mode always on for Codex-session replies instead of gating it behind task-phrase classification.

## Method

TDD

## Acceptance Criteria

- managed `telegram-codex.check-message <session-id>` opens the verbose trace for every Codex-session reply, including conversational follow-up messages like `These make it better`
- the initial `Resuming active Codex session` kickoff line still appears before richer streamed Codex events arrive
- managed reply failure paths still keep the typing guard cleanup correct while attempting the initial verbose trace first
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
