# DD-298

## Summary

Harden the managed Telegram reply worker so mid-progress reporter failures and Codex subprocess failures do not silently cut off the active Telegram operation, and add a per-session audit trail that can be enabled from `dashboard telegram-codex.start --audit`.

## Method

TDD

## Acceptance Criteria

- a failed Telegram verbose progress update no longer aborts the active managed Codex reply mid-operation
- `dashboard telegram-codex.start --audit` enables a per-session audit trail under `~/.telegram-codex/<session-id>/`
- the managed `codex exec resume` path records enough audit detail to diagnose exit status, stderr tail, and progress callback failures
- managed Codex reply failures no longer collapse into a generic message when real subprocess exit context exists
- docs explain the audit trail and failure-handling behavior
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
