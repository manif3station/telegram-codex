# DD-279

## Summary

Make `dashboard telegram-codex.start` provide stable two-way Telegram communication by auto-acknowledging new inbound messages once and keeping the listener alive across transient Telegram transport failures.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start` launches the Telegram listener with an explicit concise acknowledgement reply so new inbound Telegram messages receive one immediate bot response
- direct `dashboard telegram-codex.listen` remains passive by default unless reply text is explicitly passed
- the listener survives transient Telegram `getUpdates` transport failures instead of exiting immediately and losing two-way communication
- if the stored offset falls behind the inbox ledger, the listener advances to the newer recovered offset instead of replaying duplicated older Telegram updates
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
