# DD-291

## Summary

Keep Telegram `typing...` visible through final delivery and stream live in-progress status updates into Telegram while the resumed Codex session is still working on a longer task request.

## Method

TDD

## Acceptance Criteria

- the collector-owned `dashboard telegram-codex.check-message <session-id>` worker keeps Telegram `typing...` active until the final outbound reply send attempt finishes
- the managed Telegram path can stream short in-progress status updates from this Codex session into Telegram while longer work is still ongoing
- those progress updates do not replace the final substantive reply and do not replay old messages
- docs explain the managed typing and progress-streaming behavior clearly
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
