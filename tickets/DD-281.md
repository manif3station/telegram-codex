# DD-281

## Summary

Keep `telegram-codex.listen 0 ...` running instead of exiting after one cycle so managed `telegram-codex.start` can keep its Telegram listener resident.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.listen 0 ...` treats `0` as the run-forever sentinel instead of a one-cycle cap
- managed `dashboard telegram-codex.start` keeps its listener resident because its startup path launches `telegram-codex.listen 0 30`
- docs explain the zero-cycle sentinel behavior clearly
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
