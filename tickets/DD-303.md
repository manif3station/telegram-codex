# DD-303

## Summary

Harden live Telegram pairing enforcement with explicit per-message pairing audit so the managed collector can prove whether a chat was allowed, challenged, or ignored before Codex reply work starts.

## Method

TDD

## Acceptance Criteria

- managed `dashboard telegram-codex.check-message <session-id>` records an explicit pairing decision in the session audit before any managed Codex reply work starts
- the first unpaired inbound Telegram message still gets exactly one local pairing command reply and now also records `pairing.challenge.sent` in the session audit
- paired and explicitly bypassed paths record `pairing.allowed` audit rows so live collector behavior can be diagnosed from `audit.jsonl`
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
