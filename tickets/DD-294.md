# DD-294

## Summary

Make `telegram-codex.start --version` proxy the real Codex CLI version output while keeping the path side-effect free.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start --version` returns the underlying real Codex binary version output instead of the skill version
- the `--version` CLI path stays side-effect free and does not create or restart collectors
- internal `execute_start('--version')` metadata remains available for manager callers without forcing startup side effects
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
