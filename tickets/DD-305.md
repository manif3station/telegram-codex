# DD-305

## Summary

Keep Telegram-managed replies and Codex TUI session history aligned by hydrating replies from the persisted Codex transcript and journaling Telegram turns back into that same session transcript.

## Method

TDD

## Acceptance Criteria

- managed Telegram replies hydrate from recent persisted Codex session transcript instead of only the immediate inbound Telegram text
- normalized older raw Telegram bridge prompt rows can be reused as readable shared context during later managed replies
- managed Telegram user and assistant turns are appended into the target Codex session transcript in readable form
- the shared transcript path is documented in `README.md`, `docs/`, and `AGENT.SKILL.md`
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
