# DD-304

## Summary

Replace the README command dump with a step-by-step onboarding workflow that matches the real `telegram-codex` implementation order.

## Method

TDD

## Acceptance Criteria

- `README.md` shows a getting-started workflow instead of only a flat command dump
- the documented order matches the implementation:
  - `dashboard workspace <project-id>`
  - save `TELEGRAM_BOT_TOKEN` to project `.env`
  - ignore `.env`
  - `dashboard telegram-codex.install <token>`
  - run `codex`
  - capture the Codex session id from `/status`
  - `dashboard telegram-codex.start add <codex-session-id>`
  - `dashboard telegram-codex.start [--audit]`
- `docs/usage.md` mirrors the same workflow and explicitly notes that the `start add` step must run from the same `dashboard workspace` shell so `WORKSPACE_REF` / `TICKET_REF` are available
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo

## Status

Done
