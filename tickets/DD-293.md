# DD-293

## Summary

Make `telegram-codex.start --version` return raw semver for DD launcher checks while keeping the path side-effect free.

## Method

TDD

## Acceptance Criteria

- `dashboard telegram-codex.start --version` returns a plain semver line instead of JSON so DD launcher checks can parse it
- the `--version` path stays side-effect free and does not create or restart collectors
- internal start-version manager logic remains available for non-CLI callers without forcing startup side effects
- Docker tests pass
- coverage reaches `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `telegram-codex` repo
